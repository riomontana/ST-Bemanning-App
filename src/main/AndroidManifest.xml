<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nikolapajovic.stbemanning">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/stblue"
        android:logo="@mipmap/stnegativ"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/stblue"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar" >
        <!--android:theme="@style/AppTheme">-->
        <activity android:name="com.example.nikolapajovic.stbemanning.activities.LoginActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.nikolapajovic.stbemanning.activities.CalendarActivity"
            android:screenOrientation="portrait"/>
    </application>

</manifest>